{
    "type": "module",
    "scripts": {
        "start": "node --experimental-modules server.js",
        "dev": "nodemon --experimental-modules server.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "install-render": "npm install --omit=dev && npm rebuild"
    },
    "dependencies": {
        "axios": "^1.6.7",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "firebase-admin": "^13.4.0",
        "multer": "^1.4.5-lts.1"
    },
    "engines": {
        "node": "^18.0.0"
    }
}
